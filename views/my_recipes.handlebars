<br>
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <h1>Recipe Book</h1>
                <p>This is a list of your saved recipes.</p>
                <p>Want to add a new recipe? <a href="/choose_recipe"> You can do that here.</a></p>
                <p>Want to build your own recipe? <a href="/build_recipe"> You can do that here.</a></p>
            </div>
        </div>
        <div class="row">
            <div class="col-md-9">
                <h2>Saved Recipes</h2>
                <table class="table table-hover table-striped">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Date Added</th>
                            <th>Ethical Impact</th>
                            <th>View/Edit</th>
                            <th>Delete</th>

                        </tr>
                    </thead>
                    <tbody>
                        {{#each myRecipes}}
                        <tr>
                            <td>{{this.name}}</td>
                            <td>{{this.date}}</td>
                            <td ><span class="badge badge-{{this.impact_color}}">{{this.impact}}</span></td>
                            <td>
                                <form action='/view_ingredients' method="get">
                                    <div class="form-group">
                                        <button type="submit" name="recipe_id" value="{{this.id}}" class="btn btn-primary">View/Edit</button>
                                    </div>
                                </form>
                            </td>
                            <td>
                                <a href= #><button type="button" class="btn btn-danger">Delete</button></a>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div> 